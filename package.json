{
    "name": "floof-game",
    "version": "v1.0.0",
    "description": "The game source for floof.io!",
    "main": "index.js",
    "type": "module",
    "scripts": {
        "build": "webpack --config ./webpack.config.js",
        "deploy": "bun run build && rsync -av --delete ./build/ /var/www/floof.supercord.dev/",
        "deploy:dev": "bun run build && rsync -av --delete ./build/ /var/www/dev.floof.supercord.dev/",
        "watch": "webpack --watch --mode development"
    },
    "dependencies": {
        "dotenv": "^17.2.3",
        "html-webpack-plugin": "^5.6.4",
        "js-beautify": "latest",
        "uglify-js": "latest",
        "webpack": "^5.78.0",
        "webpack-cli": "^4.8.0"
    },
    "engines": {
        "node": ">=16.0.0"
    },
    "devDependencies": {
        "terser-webpack-plugin": "^5.6.1"
    }
}
